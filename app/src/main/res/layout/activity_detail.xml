<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:clipToPadding="false"
    tools:context="rocks.morrisontech.historicsf.DetailActivity">

    <TextView
        android:text="@string/building_name_default_text"
        android:layout_height="wrap_content"
        android:id="@+id/buildingNameTextView"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:textSize="24sp"
        android:contentDescription="@string/building_name_default_text"
        android:layout_width="300dp" />

    <ImageView
        android:id="@+id/buildingImageView"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_below="@+id/buildingNameTextView"
        android:layout_alignParentStart="true"
        android:elevation="10dp"
        android:background="@color/colorPrimaryDark"
        android:contentDescription="@string/thumbnail_image_content_string"/>
</RelativeLayout>
