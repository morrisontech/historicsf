<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="rocks.morrisontech.historicsf.DetailActivity">

    <TextView
        android:text="Building Name"
        android:layout_height="wrap_content"
        android:id="@+id/buildingNameTextView"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:contentDescription="Building Name"
        android:textAppearance="@android:style/TextAppearance.Material.Large"
        android:layout_width="200dp" />

    <ImageView
        android:layout_height="300dp"
        app:srcCompat="@drawable/ic_image_black_24px"
        android:layout_marginTop="24dp"
        android:id="@+id/buildingImageView"
        android:layout_width="300dp"
        android:layout_below="@+id/buildingNameTextView"
        android:layout_alignParentStart="true" />
</RelativeLayout>
